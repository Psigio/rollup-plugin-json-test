{
  "name": "rollup-plugin-json-test",
  "version": "1.0.0",
  "description": "Reproduces an issue with using a variable called name with rollup-plugin-json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/rollup --config=./rollup.config.js --input ./src/main.js --output dist/bundle.js; python -m SimpleHTTPServer"
  },
  "author": "Jon D",
  "license": "ISC",
  "devDependencies": {
    "rollup": "^0.34.10",
    "rollup-plugin-commonjs": "^3.3.1",
    "rollup-plugin-json": "^2.0.1",
    "rollup-plugin-node-resolve": "^2.0.0"
  }
}
